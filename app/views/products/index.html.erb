<h1>Products#index</h1>
<h2>
<%=	notice %>
</h2>
<table class="table">
	<tr>
		<th> Id Product </th>
		<th> Referencia </th>
		<th> Name </th>
				
	</tr>

	<% @products.each do |product| %>
	<tr>
		<td><%= product.id%></td>
		<td><%= product.ref%></td>
		<td><%= product.name%></td>
		<td><%= link_to 'Ver detalle', url_for(controller: :products, action: :show, id: product.id) %>
			<%= link_to 'Editar Producto', edit_product_path(product) %>
		<%= link_to 'Eliminar', product, method: :delete, data: {confirm: '¿Deseas Eliminar el Producto?'} %></td>
	</tr>

	<% end %>

</table>
